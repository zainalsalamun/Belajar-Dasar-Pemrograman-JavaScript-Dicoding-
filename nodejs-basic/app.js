// const server = new Server({
//     host: process.env.NODE_ENV !== 'production' ? 'localhost' : 'dicoding.com'
// })

const firstName = process[2];
const lastName = process.argv[3];

console.log(`Hello ${firstName} ${lastName}`);